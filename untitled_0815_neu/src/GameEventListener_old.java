/**
 * Dieses Interface beschreibt die Methode, mit der ein Event von einem Listener empfangen und bearbeitet wird.
 * @author Bjoern List
 *
 */
import utilities.*;

public interface GameEventListener_old{

		public void handleEvent(GameEvent event);
}
